노드는 타입 넘버
타입 넘버에 따른 실 구현은 각 타일에서 처리 -> 스크립터블 오브젝트로 처리

게임 스타트 -> 초기화(노드 배치 ->타일 생성(각 노드의 타입으로 변환) -> 매치되는 타일이 있는지 체크 -> 있다면 타입 바꿔줌 -> 대기 상태로 전환)
 -> 대기(플레이어 입력 대기 -> 입력시 스왑 -> 매치되는 타일 있다면 이벤트 상태로 전환 없다면 다시 스왑하고 대기)
-> 이벤트(매치 된 타일 제거 및 재생성 -> 매치되는 타일 체크 -> 있다면 다시 이벤트 없다면 대기상태)
